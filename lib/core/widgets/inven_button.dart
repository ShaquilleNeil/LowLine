import 'package:flutter/material.dart';

// Shared button component — placeholder.
class InvenButton extends StatelessWidget {
  const InvenButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

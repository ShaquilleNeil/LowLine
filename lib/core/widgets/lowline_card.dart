import 'package:flutter/material.dart';

// Shared card component — placeholder.
class LowLineCard extends StatelessWidget {
  const LowLineCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
